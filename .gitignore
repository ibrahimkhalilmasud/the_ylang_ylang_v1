# Dependencies
node_modules/
.pnpm-store/

# Next.js (when scaffolded)
.next/
out/
build/
dist/

# Env
.env
.env*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Test output
coverage/
playwright-report/
test-results/

# next-env is generated
# (kept in repo per Next convention — do not ignore)

# Raw asset originals are large; optimized derivatives live in public/media.
# Keep originals out of the deploy bundle but available locally.
# (Comment out the next line if you want originals committed.)
# asset/
node_modules_old*/

# We use pnpm — ignore npm/yarn lockfiles
package-lock.json
yarn.lock

# TS build cache
*.tsbuildinfo
