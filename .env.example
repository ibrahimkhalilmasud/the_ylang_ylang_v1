# Booking provider selection. Default (unset) = real enquiry provider.
# 'mock' enables DEV-ONLY demo availability (clearly labelled in the UI).
# 'external' is a placeholder for a future real availability API.
# NEXT_PUBLIC_BOOKING_PROVIDER=enquiry
