# Windows-friendly install: avoid symlinks (which require Developer Mode / admin and
# otherwise fail with ERR_PNPM_EISDIR). Hoisted linker copies packages instead.
node-linker=hoisted
shamefully-hoist=true
